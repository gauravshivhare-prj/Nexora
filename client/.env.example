# Copy this file to .env and adjust if your backend runs elsewhere.
# Only VITE_-prefixed variables reach the browser — never put a secret here.

# Base URL of the Nexora backend. No trailing slash.
VITE_API_URL=http://localhost:5000
